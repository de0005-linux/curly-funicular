# 🌐 تنظیم آی‌پی خروجی (ProxyIP / Relay IP)

این قابلیت همان مکانیزم **ProxyIP** ریپازیتوری `cmliu/edgetunnel` است که داخل EMS11 پیاده‌سازی شده.
هدف: آی‌پی‌ای که **سایت‌های مقصد** می‌بینند، آی‌پی سرور تو نباشد بلکه آی‌پی انتخابی خودت باشد.

---

## 🧠 اول از همه: ProxyIP دقیقاً چه کاری می‌کند؟

مسیر ترافیک بدون ProxyIP:

```
کلاینت → کلادفلر/دامنه → سرور EMS11 → سایت مقصد
                                     ▲ سایت، آی‌پی سرور EMS11 را می‌بیند
```

مسیر ترافیک با ProxyIP:

```
کلاینت → کلادفلر/دامنه → سرور EMS11 → ProxyIP (ریلی معکوس) → سایت مقصد
                                                            ▲ سایت، آی‌پی ProxyIP را می‌بیند
```

سرور EMS11 به‌جای اتصال مستقیم به مقصد، به یک **ریلی معکوس** وصل می‌شود و
«بسته‌ی اول» (first packet) را **خام و دست‌نخورده** برای آن می‌فرستد. ریلی با خواندن
`SNI` داخل همان بسته می‌فهمد مقصد کجاست و اتصال را از طرف خودش برقرار می‌کند.
نتیجه: **exit IP همان ProxyIP است.**

---

## ⚡ روش تنظیم (پنل وب)

1. وارد داشبورد شو → بخش **Settings**
2. کارت **Exit IP · ProxyIP**
3. مد را روی **ProxyIP** بگذار
4. در «لیست ProxyIP» مقصدها را وارد کن
5. **ذخیره‌ی تنطیمات** و بعد **تست اتصال**

تنظیمات بلافاصله و **بدون ری‌استارت** روی همه‌ی اتصال‌های جدید اعمال می‌شود و در
`x4g_state.json` ذخیره می‌شود (پس بعد از ری‌دیپلوی هم باقی می‌ماند).

## 🤖 روش تنظیم (ربات تلگرام)

`/admin` → **🌐 آی‌پی خروجی (ProxyIP)**

همان‌جا می‌توانی مد را عوض کنی، لیست ProxyIP یا آدرس پروکسی را ویرایش کنی،
`fallback` و «سراسری» را روشن/خاموش کنی و **تست اتصال** بگیری.

---

## 📝 فرمت‌های پذیرفته‌شده در لیست ProxyIP

همه‌ی این حالت‌ها (مثل مرجع) پشتیبانی می‌شوند و با **کاما، فاصله یا خط جدید** جدا می‌شوند:

| ورودی | تفسیر |
|---|---|
| `proxyip.example.com` | پورت پیش‌فرض `443` |
| `proxyip.example.com:8443` | پورت `8443` |
| `1.2.3.4` | آی‌پی v4 با پورت `443` |
| `[2606:4700::1]:2053` | آی‌پی v6 با پورت `2053` |
| `[2606:4700::1]` | آی‌پی v6 با پورت `443` |
| `ts.example.com.tp8443` | پسوند `.tpNNNN` → پورت `8443` |
| `a.com:443#tokyo` | برچسب بعد از `#` نادیده گرفته می‌شود |
| یک دامنه با رکورد TXT | رکورد TXT باز می‌شود و لیست داخلش استخراج می‌شود |

نمونه‌ی کامل:

```
proxyip.example.com:443
1.2.3.4
[2606:4700::1]:2053
ts.example.com.tp8443
```

> از هر لیست، حداکثر **۸ کاندید** انتخاب می‌شود. انتخاب **قطعی (deterministic)** است:
> برای یک مقصد مشخص همیشه همان ترتیب می‌آید، پس exit IP بی‌دلیل بالا-پایین نمی‌شود.

---

## 🎛 بقیه‌ی گزینه‌ها

| گزینه | معادل در مرجع | کارش چیست |
|---|---|---|
| دایال موازی | `PROXY_CONCURRENT_DIAL` | چند کاندید هم‌زمان تست شوند؛ اولین موفق برنده. سریع‌تر وصل می‌شود ولی exit IP کمتر ثابت می‌ماند. `1` = پایدارترین |
| fallback مستقیم | `反代兜底` | اگر همه‌ی کاندیدها خطا دادند، مستقیم وصل شود. **خاموش** = اتصال قطع می‌شود و آی‌پی سرور هرگز لو نمی‌رود |
| سراسری | `PROXY_GLOBAL` | پروکسی زنجیره‌ای برای همه‌ی مقصدها |
| میزبان‌های اجباری | `GO2SOCKS5` | فقط این دامنه‌ها از پروکسی رد شوند؛ مثل `*.ip111.cn,*google.com` یا `*` برای همه |

---

## 🔗 مدهای دیگر: SOCKS5 / HTTP / HTTPS

اگر جای ریلی معکوس، یک **پروکسی واقعی** داری:

```
socks5://user:pass@1.2.3.4:1080
http://1.2.3.4:3128
https://user:pass@proxy.example.com:443
```

تفاوت مهم با ProxyIP:

- **ProxyIP** آدرس مقصد را از `SNI` می‌خواند → عملاً برای **TLS روی 443** مناسب است.
- **SOCKS5 / HTTP CONNECT** آدرس واقعی مقصد را صریحاً اعلام می‌کند → روی **هر پورت و هر پروتکلی** کار می‌کند و exit IP تضمین‌شده‌تر است.

پس اگر می‌خواهی *همه‌ی* ترافیک (نه فقط HTTPS) از آی‌پی دلخواه خارج شود،
مد **SOCKS5** را با گزینه‌ی «سراسری» روشن انتخاب کن.

---

## 🎯 تنظیم اختصاصی برای یک کانفیگ خاص

می‌توانی برای هر کانفیگ، آی‌پی خروجی جدا بدهی (بقیه دست‌نخورده می‌مانند):

```
PATCH /api/links/{uuid}
{ "proxyip": "1.2.3.4:443" }
```

یا کامل:

```
PATCH /api/links/{uuid}
{ "outbound": { "mode": "socks5",
                "proxy_url": "socks5://u:p@1.2.3.4:1080",
                "global_proxy": true } }
```

برای حذف override، مقدار خالی بفرست.

---

## 🔧 متغیرهای محیطی (اختیاری)

اگر ترجیح می‌دهی از Railway مقدار بدهی. **توجه:** تنظیمات پنل/ربات روی این‌ها اولویت دارند.

| متغیر | کارش |
|---|---|
| `PROXYIP` | لیست ProxyIP |
| `PROXY_CONCURRENT_DIAL` | تعداد دایال موازی |
| `SOCKS5` یا `OUTBOUND_PROXY` | آدرس پروکسی زنجیره‌ای |
| `GO2SOCKS5` | لیست میزبان‌های اجباری |
| `OUTBOUND_MODE` | `direct` / `proxyip` / `socks5` / `http` / `https` |
| `PROXYIP_FALLBACK` | `1` برای روشن کردن fallback |
| `PROXY_GLOBAL` | `1` برای پروکسی سراسری |

---

## 🧪 تست اتصال

دکمه‌ی **تست اتصال** هر کاندید را واقعاً با TCP می‌زند و زمان پاسخ را نشان می‌دهد:

```
mode      : proxyip
target    : www.cloudflare.com
candidates: 2
  [1] 1.2.3.4:443        ->  OK  38 ms
  [2] proxyip.example.com:443 ->  FAIL timeout

reachable : 1 / 2
```

برای دیدن exit IP نهایی، بعد از اتصال با کلاینت، یکی از این‌ها را باز کن:
`ip111.cn` یا `whatismyipaddress.com`.

---

## ⚠️ محدودیت‌های صادقانه

- **ProxyIP یک سرویس بیرونی است.** EMS11 آن را نمی‌سازد؛ فقط به آن وصل می‌شود.
  اگر ریلی‌ات down باشد یا کیفیت پایینی داشته باشد، سرعت و پایداری افت می‌کند.
  کیفیت exit IP کاملاً به ریلی انتخابی تو بستگی دارد.
- **ProxyIP با SNI مسیریابی می‌شود.** برای ترافیک غیر-TLS یا بدون SNI جواب نمی‌دهد.
  این محدودیت در خود `edgetunnel` هم وجود دارد. راه‌حل: مد SOCKS5 / HTTP CONNECT.
- **یک هاپ اضافه اضافه می‌شود.** انتظار کمی تاخیر بیشتر نسبت به مد مستقیم داشته باش.
  اگر سرعت خام مهم‌تر از آی‌پی خروجی است، مد `direct` بهترین گزینه است.
- اگر `fallback` را روشن کنی، در صورت خرابی همه‌ی کاندیدها ترافیک **مستقیم** می‌رود و
  سایت‌ها آی‌پی سرور را می‌بینند. اگر مخفی‌ماندن آی‌پی سرور برایت مهم است، خاموش نگهش دار.

---

## اگر همه‌ی کانفیگ‌ها پینگ ‎-1‎ دادند

این یعنی آی‌پی خروجی ذخیره شده و مد روی `proxyip` است، ولی سرور ریلی
ترافیک را واقعاً فوروارد نمی‌کند.

یک آی‌پی می‌تواند «سالم» باشد و پورت ۴۴۳ آن باز باشد، ولی ProxyIP نباشد.
ProxyIP یک reverse proxy است که SNI را از ClientHello می‌خواند و به همان
مقصد فوروارد می‌کند. اگر سرور فقط پورت را باز کرده باشد (مثلاً یک وب‌سرور
معمولی یا یک پنل)، اتصال TCP برقرار می‌شود ولی هیچ داده‌ای برنمی‌گردد و
تونل تا ابد منتظر می‌ماند؛ کلاینت این را `-1` نشان می‌دهد.

### چه چیزی در این نسخه عوض شد

1. **نگهبان اولین بایت:** بعد از فرستادن ClientHello به ریلی، حداکثر
   `PROXYIP_VERIFY_TIMEOUT` ثانیه (پیش‌فرض ۶) منتظر اولین بایت پاسخ
   می‌مانیم. اگر چیزی نیامد، آن کاندید **ناموفق** حساب می‌شود:
   - کاندید بعدی امتحان می‌شود،
   - و اگر `fallback` روشن باشد، اتصال مستقیم برقرار می‌شود (پینگ سالم،
     ولی آی‌پی خروجی همان سرور است).
   قبلاً موفقیت TCP کافی بود، پس `fallback` هرگز فعال نمی‌شد.

2. **این نگهبان فقط برای TLS فعال است.** اگر بسته‌ی اول ClientHello نباشد
   (پروتکل‌هایی که اول سرور صحبت می‌کند)، انتظار برای پاسخ اشتباه است، پس
   نگهبان اجرا نمی‌شود.

3. **تست اتصال پنل واقعی شد.** قبلاً فقط TCP را چک می‌کرد و همین باعث شد
   یک ریلی خراب «سالم» گزارش شود. حالا یک دست‌دهی کامل TLS با SNI مقصد
   انجام می‌دهد:
   - `OK ... + relay/TLS ... ms` → ریلی واقعاً کار می‌کند.
   - `TCP OK ... -> RELAY FAIL: ...` → پورت باز است ولی فوروارد نمی‌کند.

### راه‌حل‌ها

- یک ProxyIP واقعی بگذار (لیست‌های عمومی ProxyIP یا سروری که خودت با
  SNI-proxy بالا آورده‌ای).
- یا مد را `SOCKS5` / `HTTP` کن. این مدها آدرس مقصد را صریح می‌فرستند،
  پس به SNI وابسته نیستند و روی هر پورت و هر پروتکلی کار می‌کنند.
- برای خاموش‌کردن نگهبان: `PROXYIP_VERIFY_TIMEOUT=0`.
